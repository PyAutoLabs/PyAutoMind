The project @z_projects/ic50_workspace is our IC50  use case which we are now aiming to scale up the EP framework
to the IC50 use case.

Currently, all IC50 fits use DynestyStatic, which is slow because its nested sampling and unecessarily
complicated given its fitting a simple 3 parameter model.