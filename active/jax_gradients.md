The folder @autolens_workspace_developer/jax_profiling illustrates tests of the 
key likelihod functions in autolens using jax.jit, step-by-step, but it omits gradients.

Can you make a complete step-by-step script for testing the gradients of the mge likelihood function,
so I can work out if it gives gradients and if not which step is the problem.