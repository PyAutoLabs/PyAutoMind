The folder @z_staing/dspl contains some example SLaM pipelines for double source plane lens modeling.

However, they are on older code API, are a bit clunky and need polish.

Can you first compare them to the new API, I think @autolens_workspace/scripts/guides/modeling/slam_start_here.py
is a good point of comparison. Can you then try and make the DSPL pipelines follow the new API wherever
possible, give me a plan for how to do this.

Once we are in sync with the new API we can then do some test runs and figure out how to imporve
them from here.