Currently, subplot_fit is 4 x 3 panels, with two source reconstructions in the middle-right and bottom-right row.

However, it is common for the Zoom panel to over zoom, making it hard to judge the fit.

Equally, the Data (Source Scale) panel is verging on useless.

Therefore, can we:

1) Move Model Image to where Data (Source Scale) is and remove Data (Scouece Scale).
2) Move Source Plane (Zoomed) up one to the top right and rename its title (Max Zoom).
3) Make a new panel, Source Plane (Mid Zoom), which uses the same centre as Max Zoom but expands the y axia and x axis to be 2.5x larger than Max Zoom.
