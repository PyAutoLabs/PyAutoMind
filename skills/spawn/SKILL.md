---
name: spawn
description: Regenerate and optionally publish the PyAutoMind and PyAutoMemory fresh-slate template repositories from their live sources. Use for template drift checks or a human-confirmed template spawn, never for ordinary repository cloning.
---

# Spawn

Follow [`spawn.md`](spawn.md) exactly. Run the dry-run and canary scan before any
write, and preserve the documented human gate around `--apply`.
