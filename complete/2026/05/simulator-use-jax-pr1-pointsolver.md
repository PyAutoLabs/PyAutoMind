## simulator-use-jax-pr1-pointsolver
- issue: https://github.com/PyAutoLabs/PyAutoArray/issues/334
- completed: 2026-05-24
- library-pr: https://github.com/PyAutoLabs/PyAutoLens/pull/538
- notes: PR 1 of 4 from the simulator_use_jax.md prompt. Shipped `al.PointSolver(use_jax=True)` constructor flag (forwarded by for_grid / for_limits_and_scale), `solve()` auto-defaults for `xp` and `remove_infinities` from `self._xp`, and new `autolens.jax.register_tracer_classes(tracer)` public helper. The walker mirrors `AnalysisImaging._register_fit_imaging_pytrees()` for the standalone (non-Analysis) JIT-it-yourself path. **Design correction surfaced during implementation:** Phase 0 design doc said "user never imports register_*" — wrong; JAX flattens function args at trace time before entering the decorated function, so auto-registration inside `solve()` cannot work for `@jax.jit def f(tracer, coord)`. Users must call `register_tracer_classes(tracer)` once before the first jitted call. One import + one call is acceptable since JAX is opt-in for performance. Phase 0 design doc §3.1 / §3.4.1 should be amended on the next pass. Issue PyAutoArray#334 stays OPEN until PRs 2-4 ship. **Remaining work (PRs 2-4):** (2) `SimulatorImaging.use_jax=True` + xp-conversion of internals (Poisson noise needs jax.random.PRNGKey from noise_seed); (3) `SimulatorInterferometer.use_jax=True` + via_image_from(xp=...) signature symmetry fix; (4) `xp=np + jnp-backed-grid` mismatch `ValueError` in PyAutoArray's `AbstractMaker.__init__`. Plus a follow-up workspace_test PR with `solver_use_jax_parity.py` (already authored at canonical path, currently untracked, depends on this library API being released). Worktree removed; feature branches deleted local + origin.
