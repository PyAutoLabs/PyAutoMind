## mcmc-corner-smoke
- completed: 2026-07-10
- prs: PyAutoFit#1347 (merged, pending-release) + autolens_workspace_test#159 (merged, pending-release)
- summary: The `searches/mcmc.py` "no dynamic range" release-smoke crash (nightly-release blocker) was a **library** bug, not autofit_workspace as the prompt assumed. Reproduced on main via PYAUTO_TEST_MODE=1: `corner_cornerpy` (autofit/non_linear/plot/samples_plotters.py) called `corner.corner(...)` with no `range=`, so any degenerate (zero-spread) column raised. Fix = `_corner_range_from(data)` helper computes explicit per-column ranges (widening degenerate columns), passed as `range=`; affects every corner-plotting search. New unit test in test_autofit/non_linear/plot. Verified mcmc.py exits 0 + writes corner.png. PR#1347 (PyAutoFit) shipped this. autolens_workspace_test#159 rode along in the same session: the release-matrix `*/modeling_visualization_jit.py` Part-2 "no fit.png produced" failures on the LENS scripts (imaging/interferometer/point_source) were a filename mismatch — the lens quick-update writes `fit_quick.png` (subplot_fit_quick), the assertion globbed `fit.png`; fixed the glob, verified against a real produced artifact. NOTE: this only covers the lens viz scripts. The GALAXY imaging/interferometer + ellipse viz failures do NOT reproduce on current source main — fresh smoke-mode matrix 29082977784 (1059 pass / 24 fail / 124 skip): autogalaxy_test = 51 pass / 3 fail with 0 viz among the failures, so they pass on current main; the 3-day release-run failures were stale or release-wheel-specific (old floor wheels 2026.7.6.649). No source fix warranted; re-validate in the next release-fidelity run. The `*/modeling_visualization_jit.py` scripts are SLOW-skipped in routine smoke/schedule mode (autolens variants explicitly, >300s cap) so they only run in the RELEASE gate. The Part-1 `warm_dt < 0.1` perf assertion is hardware-flaky (339ms on laptop; point_source's already fixed to a cold/warm ratio). See memory project_heart_yellow_mcmc_viz_fixes. The broader ~37 matrix failures (incl. 9-strong TFP↔JAX pytype_aval_mappings dep incompat) remain out of scope; Heart stays yellow.
