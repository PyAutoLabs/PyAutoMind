## starred-frame-epsf
- issue: https://github.com/PyAutoLabs/PyAutoReduce/issues/41 (closed)
- completed: 2026-07-11
- library-pr: https://github.com/PyAutoLabs/PyAutoReduce/pull/42 (merged, squash; branch+worktree cleaned)
- repos: PyAutoReduce
- summary: Extended STARRED Tier-1b (#36) to the per-exposure FRAME-PRODUCTS mode. build_starred_frame_epsf (autoreduce/psf/starred_epsf.py): native-pixel STARRED ePSF for ONE exposure — NO drizzle-consistency resample (native distorted grid), per-star noise from the frame ERR ext, returns (None,None,diag) on few-stars like photutils build_frame_epsf, loud StarredUnavailableError if extra absent. Refactored shared _prepare_frame helper (frame_epsf.py) so both back-ends use ONE identical star-selection path (returns work,err,found,n_patched,primary,target_xy,det_shape — the extra 3 for the JWST tier-2b fallback). Dispatch package/frames._frame_psf on spec.psf_backend. 3 new tests (ERR context, missing-extra guard, dispatch route); 209 pass. VALIDATED on real frames (prototypes/starred_vs_epsf_frame.py, #41 comment-4948745553): STARRED vs photutils per-frame ePSF vs empirical frame-star stack — Omega Cen WFC3/UVIS FLC (empirical conc 0.69 sharp → STARRED 0.31 BROADENED, photutils conc>1 NEGATIVE WINGS) + M92 NIRCam SW CAL (empirical 0.34 → STARRED 0.33 MATCHES, photutils 0.34 + neighbour-contamination blob). STARRED wins BOTH by radial RMS (rejects photutils per-frame pathologies) but BROADENS sharp native PSFs → CONDITIONAL/MARGINAL upgrade per-frame, NOT the clear mosaic win; photutils Tier-1 stays a sound frame default. Native single frames undersampled by construction + native-pixel delivery discards STARRED super-res. KEY: build_starred_frame_epsf does star selection INTERNALLY (find_stars→photutils) so needs photutils+starred COEXISTING (pyautoreduce[starred] env; photutils is base dep>=1.8, verified coexist w/ astropy8+jax; mosaic path hid this by pre-selecting stars in PyAuto venv). Traps: worktree caches deleted on cleanup → re-download frames; build_starred_frame_epsf runs in ~/venv/starred (now w/ photutils) + autoreduce PYTHONPATH. Regime map now spans mosaic (WFC3 win / JWST-SW loss / JWST-LW win) + per-frame (both conditional). Siblings: project_starred_epsf_backend.md (#36/#37/#39).
