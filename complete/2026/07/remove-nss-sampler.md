## remove-nss-sampler (ALL phases MERGED + dev-clean)
- issue: https://github.com/PyAutoLabs/PyAutoFit/issues/1356 (ready to CLOSE — every phase merged 2026-07-11; final workspace-wide sweep confirms zero functional NSS)
- dev-clean: final sweep caught leftover searches_minimal/ NSS prototype scripts in 2 dev workspaces (3 were BROKEN, imported removed lib) → deleted all 10 + tracked outputs + CLAUDE.md refs: autofit_workspace_developer#21 + autolens_workspace_developer#92 (MERGED). Kept: sibling sampler scripts' comparative mentions + findings docs (historical).
- completed: 2026-07-11
- prs: PyAutoFit#1357 + autofit_workspace#88 + autofit_workspace_developer#20 (core, library-first) + PyAutoFit#1360 (orphan nss_install_smoke.yml) + PyAutoBuild#146 + PyAutoHeart#60 + PyAutoFit#1361 (Phase 3/4/straggler) + autolens_profiling#65 (Phase 5) — ALL MERGED. Phase 6 (retire nss_first_class_sampler epic + void 8 issued/nss_* prompts) inline on Mind main.
- remainder: Phases 3/4/6 shipped once codex-organ-skill-wrappers merged (freed Build/Heart/Mind); Phase 5 (autolens_profiling#65) once profiling tasks #63/#64 merged — removed searches/nss/ + 9 sweep cells + build_nss/_NSS_DEFAULTS/registration + _runner branch + 3 A100 HPC submits + README/hpc doc refs (26 tests pass); KEPT results/notes A100 data + PyAutoNSS HPC venv (drives all GPU profiling). Judgment calls (flagged in PRs): KEPT the general direct-reference-URL packaging guard (PyAutoBuild) + wheel-install doc requirement (PyAutoHeart) — de-NSS'd not deleted (useful beyond NSS).
- summary: Removed the NSS nested slice sampler (`af.NSS`) and its infrastructure — bespoke git+ install / CI / build machinery not justified by measured perf (faster per-eval on MGE but OOM-prone on pix/delaunay via vmap fan-out); can return as a real pip install later. PyAutoFit#1357: deleted autofit/non_linear/search/nest/nss module+tests, af.NSS export, [nss] extra + git+ footgun comment block, AGENTS notes (1393-line del, PUBLIC API removal); blackjax RETAINED (BlackJAXNUTS uses it). autofit_workspace#88: removed the Search:NSS tutorial section from scripts/searches/nest.py + regenerated notebook (22→20 cells). autofit_workspace_developer#20: PRESERVED the impl at searches/nss/ (alongside ultranest/pyswarms) — module relativized as drop-in, tests verbatim vs library paths, README with pinned handley-lab/blackjax@ef45acd2 + yallup/nss@69159b0f SHAs + re-mainline checklist, example.py. Gate: test_autofit 1471p/1s/0f; downstream galaxy/lens import-clean; review CLEAN; Heart YELLOW (ambient non-NSS, human-acked in-session). --auto supervised. Worktree + local branches cleaned up. Trap: arviz entered the stack ONLY via nss (Required-by: nss) — removal sheds that footgun.
