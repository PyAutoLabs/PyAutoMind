## import-time-heart-leg (hygiene follow-up — complete)
- issue: PyAutoHeart#61 (closed)
- completed: 2026-07-11
- prs: PyAutoHeart#62 (import_time leg) + PyAutoBrain#93 (perf consults it) — both MERGED 2026-07-11 (squash), --auto supervised, live sign-off
- summary: deep-researched then built the standing `import_time` PyAutoHeart leg that promotes the hygiene perf mode's import pre-scan to a tracked signal. Research changed the design: (1) measured `import autolens` ≈ several sec ≫ the <30s tick budget (internals rule 3) → **OFF-TICK** (daily cron / on-demand `python -m heart.checks.import_time`, NOT wired into tick.sh); (2) rule 4 (stdlib-only Heart) → **subprocess** measurement + **injected measurer** so neither Heart's process nor its tests import the science stack; (3) mirrors script_timing (rolling baseline + ratio 1.5/3.0 classify) and url_check (**advisory** dashboard section, NOT in the readiness gating set — import time isn't release-blocking). heart/checks/import_time.py + state.aggregate + dashboard section + 7 stdlib tests (251p). PyAutoBrain#93: hygiene perf prefers the leg's import_time.json when present, subprocess scan as graceful fallback (no hard merge-order dep). Traps: /usr/bin/time prints elapsed even on FAILED import (autolens not in ~/venv/PyAuto — only the lower 4 libs are); git add -A grabbed a stray empty root.log (gitignored). 2 calibration rows. Closes the hygiene arc.
