## weak-modeling
- issue: https://github.com/PyAutoLabs/PyAutoLens/issues/579
- completed: 2026-07-09
- library-pr: https://github.com/PyAutoLabs/PyAutoLens/pull/580 (merged)
- workspace-pr: https://github.com/PyAutoLabs/autolens_workspace/pull/241 (merged)
- notes: Step 4 (keystone) of the weak-lensing series, run under --auto supervised end to end in one session: intake -> plan-to-issue -> implement -> park at ship sign-off -> human approve -> PR-open -> CI -> merge -> close. Added `al.AnalysisWeak` + `autolens/weak/model/{analysis,result,plotter,visualizer}.py` mirroring `point/model` (NOT imaging/model — FitWeak follows FitPoint's standalone pattern; the prompt's "mirror imaging" was adapted). `use_jax=False` default: FitWeak is NumPy-only (cached_property + np.asarray, no xp threading); JAX needs pytree registration — deliberate future work. New `weak_dataset`/`fit_weak` sections in packaged visualize/plots.yaml, mirrored into autolens_workspace config. Workspace `scripts/weak/modeling.py` validated with a REAL ~7-min Nautilus run: einstein_radius 1.556 (1.476-1.631) vs truth 1.6; the search's own VisualizerWeak wrote on-the-fly subplots. Gotchas: (1) `autolens/__init__.py` is CRLF — a scripted open()/write() edit ballooned the diff 317 lines; reverted and redone with newline='' (grep -c $'\r$' first, per the standing memory). (2) autolens_workspace CI "navigator / Catalogue staleness" fails on any new script until `PyAutoBuild/autobuild/regenerate_navigator.py autolens` is run and llms-full.txt + workspace_index.json committed — this is the prescribed fix, not a workaround. (3) Parallel-worktree pattern used a third time for autolens_workspace alongside lenstool-example (its diff = root metadata only). (4) Brain Feature Agent scored the task too-large/4-phase; consolidated to the series' proven 2-PR shape — right call, both PRs merged unchanged. Heart YELLOW reason set was surfaced in the sign-off question and acknowledged by the human approval. Next in series: 6_visualization_profiles + 5_likelihood_function (both unblocked), then 7 (A2744 real data), 8 (combined SL+WL), 9 (PYAUTO_SMALL_DATASETS).
