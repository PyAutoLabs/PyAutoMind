## ep-hierarchical-regression
- issue: https://github.com/PyAutoLabs/PyAutoFit/issues/1352 (closed)
- completed: 2026-07-10
- pr: https://github.com/PyAutoLabs/PyAutoFit/pull/1355 (MERGED, main de6534dfb)
- summary: Fixed the red PyAutoFit Tests workflow on main (found during a /health check green-light sweep — the only red across 2474 lib tests + 47 workspace smoke). `test_full_hierachical` is an INHERENTLY STOCHASTIC test: the Laplace optimiser refines each factor with n_refine=3 samples drawn from GLOBAL np.random (NormalMessage.sample → np.random.randn, optimiser.py:132/157 → mean_field.sample), so a marginal hierarchical EP fit converges to a good value or a sigma-collapsed one depending on ambient RNG state at fit start. The test seeded np.random only before data-gen, not the fit. #1351 (innocent trigger) added test_ep_statistics_fixes.py whose Monte-Carlo KL tests consume np.random, shifting ambient state into a failing region → suite green@#1347 (2e33b175) → red@#1351/#1354. Deep-research self-corrections: first blamed set-iteration factor ordering (WRONG — factor order is a deterministic insertion tuple); then blamed np.random alone (INCOMPLETE — fit ALSO depends on variable-id ordering via `model.variables` set iteration, so an id-offset sweep flips PASS/FAIL even with np.random seeded). Built the durable optimiser fix A (LaplaceOptimiser refine_seed param, get_state/seed/set_state around refine sampling) then REVERTED it — proven insufficient alone (A+C still flips under id-offset; A alone converges wrong). Shipped user-steered pragmatic fix: 1-line np.random.seed(0) before the fit (+comment). Gate: graphical 216 pass; full test_autofit/ 1475 pass / 14 skip / 0 fail (was 1474/1-fail at HEAD). KNOWN LIMITATION (documented on #1352, not chased per "keep it feasible"): EP/Laplace fits draw from global np.random so aren't reproducible across arbitrary process states — a controlled-RNG + deterministic-variable-ordering design is the real fix if reproducibility becomes first-class.
