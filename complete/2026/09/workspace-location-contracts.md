Phase 1a of the workspace regroup. One location contract for the organism, replacing
the private answers six organs had rolled for themselves. No directory layout changed.

## What shipped

- **PyAutoBrain#392** — `.pyauto-root` marker walk inserted ABOVE the sibling-organ probe
  in both `agents/_pyauto_root.py` and `bin/_pyauto_root.sh`; `$PYAUTO_ROOT` validated;
  `worktree_create` writes a marker into each new task bundle. 929 passed.
- **PyAutoHeart#232** — `heart/_workspace.{py,sh}` as the single door, splitting
  `workspace_root()` (locate) from `canonical_root()` (the tree health grades). ~13 sites.
  1026 passed.
- **PyAutoHands#283** — `autohands/_workspace.py` as the single door; `ADMIN_JAMMY` and an
  orphaned `PYAUTOBASE` removed. 460 passed.
- **PyAutoMind#414** — bidirectional manifest↔disk drift check; `admin_jammy` removed;
  marker generated by `--write`. 501 passed (was 482).

Integration: `repos_sync.py --write` run once from the assembled tree — root `AGENTS.md`
regenerated, `.pyauto-root` now generated, `PyAutoBrain/skills/WORKFLOW.md` owner map synced
(PyAutoBrain `9091d9e`).

## The defect fixed

`_is_root()` called a directory the workspace root when it held any of `SIBLING_ORGANS`,
without checking it was a checkout — so grouping the organs into `organs/` would make
`<root>/organs` itself satisfy the test and the root would resolve one level too deep,
silently, because every consumer degrades rather than raises. Same fixture, old vs new:

    OLD: (PosixPath('/tmp/…/organs'), 'beside this checkout')   <- one level too deep
    NEW: (PosixPath('/tmp/…'),        '.pyauto-root marker')

The change is additive — the marker walk goes above the probe, and the probe stays as a
fallback, because a remote session often checks out a single repo with no root above it and
CI lays repos out side by side with no marker. No currently-correct resolution could become
wrong.

Heart and Hands also carried a live, masked bug: `_workspace_root()` fell back to `~/Code`,
not `~/Code/PyAutoLabs`, so `root/"PyAutoBrain"` named a path that does not exist. Masked
only because it is last in a candidate chain an earlier candidate satisfies.

## The finding that shaped the phase

The scope item expected to be most theoretical — "four separate concepts, not one conflated
root" — proved itself three times in one session. All three are the same shape: **locating is
not writing, and not grading.**

1. **Heart would have graded a bundle instead of canonical.** Health is a release gate, and
   the established rule is that it grades the canonical checkout with `PYAUTO_ROOT=<worktree>`
   as the documented branch opt-in. Following the resolver inverted that: from a bundle Heart
   reported `0 orphan / 5 dirty` where canonical reports `6 orphan / 7 dirty` — the MORE
   FLATTERING answer, reached by the easy mistake of running Heart where you are working.
   Ruled back; canonical now derives from `git --git-common-dir`, not a literal, and was
   verified in all four layouts (linked worktree, canonical, symlinked repo, non-repo).
2. **Mind's `--write` would have regenerated another tree.** The resolver is Brain-anchored,
   and a bundle's `PyAutoBrain` is normally a symlink to canonical, so `--write` from a bundle
   resolved to canonical. Both adopters now refuse a root that does not contain their own
   checkout.
3. **The dashboard regen hit it during the close-out itself.** Running the canonical
   `pyauto-brain` binary from inside the bundle regenerated canonical; `--check` then said
   "are current" because it checked canonical too, while the bundle's page kept offering a
   prompt that had just been retired. Caught only by grepping the page for the slug.

## Corrections made during the work

- `spawn.py:507` was NOT a workspace-root site: that `parents[2]` lives inside a template
  string for a generated satellite repo's own smoke runner. Left alone.
- `admin_jammy` is not part of the PyAutoLabs ecosystem (human ruling, 2026-09-18), which
  made the hardest scope item simpler: it was the ONLY declared-but-absent entry, so with it
  gone "declared but missing" ships as a plain error with no opt-out key invented. 38 declared
  ↔ 38 on disk. Only the reverse direction needed one entry — `unmapped_checkouts: .github`,
  a real checkout with a real remote deliberately not in the body map — proven load-bearing by
  unlisting it and watching the workspace go red.
- The Heart scope widened from the 4 sites briefed to ~13 plus two new shim files. Converting
  only the four would have left Heart internally disagreeing, which is the defect being fixed.
- The bundle-marker heredoc in `worktree.sh` had `# PyAutoLabs task-bundle root marker.` — the
  firewall tolerated it (listed file), but it is organ code writing an instance name into every
  bundle on every machine, so the name was dropped.
- My own brief's rationale for the worktree marker was wrong (`PyAutoLabs` is not an ancestor
  of `PyAutoLabs-wt/<task>/`); the marker was kept for a better reason — a bundle genuinely is a
  root, and an organ-less bundle would otherwise report `unverified`.

## Witness

- `tests/test_workspace_root.py`: red before (7 failed against HEAD resolvers), green after
  (11 passed). Every case asserts the Python and shell halves AGREE, by running both.
- Both drift directions red-before/green-after on real repos: pulling `PyAutoReduce` aside was
  `exit=0` with 0 mentions before, `exit=1` naming it after; likewise an undeclared clone.
- Bootstrap confirmed live, not argued: fresh root → `--check` passes "presence not enforced"
  → `--write` writes the marker → next `--check` enforces. Pinned as a test.
- Post-merge on canonical: `repos_sync.py --check` exit 0 with the new leg ENFORCING;
  `workspace_root()` and `canonical_root()` both resolve to the canonical workspace.
- Acceptance for the ruling: from inside the bundle with `env -u PYAUTO_ROOT -u PYAUTO_WT_ROOT
  -u PYAUTO_BRAIN`, Heart reproduces canonical's verdict exactly; `PYAUTO_ROOT=<bundle>` still
  grades the bundle, so the documented opt-in survives.

## Found on the way, filed separately

`PyAutoHeart/heart/checks/manifest_drift.py:45` anchors its check-line parse to end-of-line, so
any leg printing a suffix after its status is INVISIBLE to Heart — not green, unparsed.
Measured against the real workspace: 16 legs parse, 1 does not (`generated hooks`), so that leg
has never reached Heart's verdict. Filed as
`draft/bug/pyautoheart/manifest_drift_parser_drops_suffixed_check_legs.md`.

## Known limitation, carried to phase 3

Step 3 of the resolver is STILL WRONG for a nested workspace with no marker, deliberately, to
protect the remote single-repo case — a test pins it. The marker is therefore the only thing
protecting a regrouped workspace: whoever performs the move must create the marker IN THE SAME
COMMIT, or resolution silently goes one level deep again.

## Next

Phase 2 — `draft/maintenance/pyautobrain/workspace_resolver_fanout.md` (the 37-copy session-start
hook, the 14 run_smoke/retime joins, the 7 hardcoded absolute paths) — was `Blocked-by:` this and
is now unblocked. Phase 3 (the physical regroup into `lens/ galaxy/ fit/ cti/ reduce/`, organs
staying flat) still waits for the in-flight tasks to clear the release gate.

## Original prompt

# Workspace location contracts: one resolver for root, repo, main and task checkouts

Type: maintenance
Target: PyAutoBrain
Repos:
- PyAutoBrain
- PyAutoHeart
- PyAutoHands
- PyAutoMind
Difficulty: large
Autonomy: supervised
Priority: high
Status: formalised
Consequence: judge
Witness: a pytest fixture builds a nested workspace (lens/PyAutoLens beside a flat PyAutoBrain) and asserts the resolver returns the TRUE root, not the family directory, for both the Python and the shell entry point; and repos_sync.py --check exits non-zero when a manifest-declared repo's directory is absent (today it passes silently, checking nothing).
Review-minutes: 25
Unattended: needs-slicing
Issued: 2026-09-18

Phase 1a of the PyAutoLabs workspace regroup. Changes NO directory layout.

## Why

The workspace root has 45 entries and is hard to navigate. The human approved (2026-09-18, in chat)
regrouping the science repos into per-family subfolders `lens/ galaxy/ fit/ cti/ reduce/`, leaving the
8 organs plus PyAutoArray, PyAutoScientist and pyautolabs.github.io flat at root. Lowercase family
names were chosen because none collides with an importable package name; the human explicitly rejected
naming a group after its library (PyAutoLens/PyAutoLens) because $ROOT/PyAutoLens would still resolve
to an existing directory after the move, converting loud failures into silent wrong-path ones.

Three read-only sweeps plus an independent Codex (gpt-6-astra) review, all 2026-09-18, established
that the blocking problem is not the move — it is that the organism has no single answer to where
anything is. Six organs disagree TODAY: PyAutoHeart/heart/_common.sh:31 hardcodes $HOME/Code/PyAutoLabs;
PyAutoHeart/heart/dashboard.py:171 and PyAutoHands/autohands/board.py:56 fall back to ~/Code (not
~/Code/PyAutoLabs); PyAutoHeart/heart/checks/worktree_drift.py:46 tests `_p3.name == "PyAutoLabs"`;
PyAutoHands/autohands/run_all.py:37 and PyAutoMind/scripts/spawn.py:507 use fixed parents[N]. Only
PyAutoBrain uses the canonical _pyauto_root pair, and that pair decides the root by probing for a
sibling organ directory (agents/_pyauto_root.py:59) without checking it is a checkout.

## Scope

1. Define four SEPARATE concepts and one API, rather than one conflated "root":
   workspace-root discovery / repo identity / main-checkout location / task-checkout location.
   This separation is the review's central recommendation: the dangerous end state is "organs resolve
   by sibling arithmetic, science resolves through a manifest". Placement must be a location property,
   not a second classification system. Note PyAutoArray stays flat but is a library, not an organ.
2. Add a `.pyauto-root` marker file at the workspace root and make root discovery walk up to it instead
   of probing SIBLING_ORGANS. Model it on autolens_profiling's profiling_root(), which already walks up
   to a ruff.toml marker and is the one layout-independent path helper in the workspace. Keep
   $PYAUTO_ROOT as an override but validate it (today _pyauto_root.py:76 bypasses every check, so a
   wrong value exported by a hook is accepted silently).
3. Make PyAutoHeart, PyAutoHands and PyAutoMind consume that single resolver instead of their six
   private answers.
4. Make the drift check work in BOTH directions, folding in the separately-filed
   `draft/maintenance/pyautomind/autolens_jax_joss_manifest_gap.md` (retire it on merge):
   - declared-but-missing is silently SKIPPED at repos_sync.py:882 (CLAUDE pointer checks),
     :1318 (origin checks) and :1607 (codex hook checks) — after any move these would pass while
     checking nothing. A successful check that means reduced coverage is the failure to remove.
   - on-disk-but-undeclared is SILENCE: repos_sync walks the manifest and asks whether each entry is
     present, so it structurally cannot see a checkout absent from repos.yaml. That is the
     manifest_gap defect, and it survived the removal of its example (autolens_jax_joss).
   Note the two are independent of presence on disk: admin_jammy is a genuine manifest entry that is
   deliberately never cloned, so "missing" must be expressible as intentional rather than as drift.
5. Record the inventory the later phases need, as committed data rather than a one-off sweep:
   existing worktrees, dependency symlinks, and runtime import locations.


## Not in this phase

The physical move, the repos.yaml `path:` field, the ~12 root enumerators, worktree.sh, the
smoke_install.sh flat pip chain, and the IDE/PYTHONPATH/symlink migration are phases 2 and 3. Do not
introduce a universal `root / manifest.path` rule here: the review's named ordering hazard is that such
a rule breaks CI immediately (whose trees are legitimately flat) and misroutes task worktrees later.
The fan-out that CONSUMES this resolver is phase 2:
`draft/maintenance/pyautobrain/workspace_resolver_fanout.md` — the 37-copy session-start hook,
the 14 run_smoke/retime PyAutoHands joins, and the 7 hardcoded absolute paths. It cannot land
first and is mechanical once this phase exists.

The dead-weight cleanup was the sibling task workspace-dead-weight-cleanup, SHIPPED
2026-09-18 (PyAutoBrain#390, PyAutoMind#413) — record
`complete/2026/09/workspace-dead-weight-cleanup.md`. Two findings from it land here:
the `.worktrees/` trees it removed are ~80 paths this phase no longer has to migrate, and
its `autolens_jax_joss_manifest_gap` sibling is the MIRROR of scope item 4 — `repos_sync.py`
is blind in both directions (declared-but-missing is skipped; on-disk-but-undeclared is
silence). Fix both together.
