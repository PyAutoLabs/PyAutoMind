JAX Modeling + Visualization:

- Need to follow up on fast_visualization.md, but waiting for all Euclid modeling runs to complete so I can begin this transition.
- Manually do Google colab checks and local checks to make sure I'm satisfied with liveness of it all.
- Then need to go through all JAX updates on workspace once the visualization is fully tested.

Cluster:

- Go to modeling, need to run existing workspace examples, give API and other feeedback and work on visualization before going further.

Weak:

- Got to source code implementation for simulator.py, need to run edisting workspace examples, then cotniue running but when we get to visualization check it all carefully. 