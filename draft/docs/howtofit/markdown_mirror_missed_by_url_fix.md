# HowToFit: the tutorial_1 markdown mirror was missed by the download-URL fix

Type: docs
Target: howtofit
Repos:
- HowToFit
Themes:
- howto
- tutorials
Difficulty: trivial
Autonomy: safe
Priority: low
Status: draft
Consequence: notify
Witness: `grep -rn "PyAutoLabs/autofit_workspace" HowToFit/markdown/chapter_1_introduction/tutorial_1_models.md` returns nothing, and the markdown mirror matches its `scripts/` source.
Review-minutes: 5
Unattended: ready
Filed: 2026-09-14

One line, left behind by an otherwise-complete fix.

`HowToFit/scripts/chapter_1_introduction/tutorial_1_models.py` carried a stale
`__Paths__` block telling a reader who wants to clone **HowToFit** to download
`autofit_workspace` — a leftover from before the HowTo series split out of the
workspace repos. It was fixed on `feature/howtofit-tutorials-1-3` (commit
`6f335b2`, pushed 2026-09-14) and now reads
`https://github.com/PyAutoLabs/HowToFit`.

The fix landed in two of the three mirrors:

- `scripts/chapter_1_introduction/tutorial_1_models.py` — fixed
- `notebooks/chapter_1_introduction/tutorial_1_models.ipynb` — fixed
- `markdown/chapter_1_introduction/tutorial_1_models.md:104` — **still stale**,
  reads `https://github.com/PyAutoLabs/autofit_workspace`

## Scope

Regenerate the markdown mirror so it matches its `scripts/` source. Do not
hand-edit `markdown/` — find why the regeneration step that updated `notebooks/`
did not also update `markdown/`, and fix the gap rather than the symptom. If
markdown mirrors are generated by a separate command from the notebook ones,
that asymmetry is the actual finding and belongs in `HowToFit/AGENTS.md`.

## Status 2026-09-14 — fixed on the branch, NOT yet on main

The regeneration was done and pushed as `ac0fd0d` on
`feature/howtofit-tutorials-1-3` (HowToFit#58), under `howto-md-rerender`
(`complete/2026/09/howto-md-rerender.md`). `markdown/chapter_1_introduction/tutorial_1_models.md`
on that branch now reads `https://github.com/PyAutoLabs/HowToFit`.

**This prompt stays open because the fix has not reached `main`.** HowToFit#58 is
another session's PR and theirs to merge. Retire this prompt when #58 merges —
nothing further to implement, only to confirm. If #58 is ever abandoned, the work
must be redone on a fresh branch, and note that on `main` both the `.py` and the
`.md` are stale, so such a branch carries both halves.

The root cause it records — `markdown/` needing a separate `generate_markdown.py`
that the documented regeneration step never runs — was fixed in neither PR and
remains a live trap across the HowTo repos.

## Why this is its own prompt

The line belongs to `feature/howtofit-tutorials-1-3`, not to
`howto-stale-self-location` (HowToGalaxy + HowToLens, issue HowToGalaxy#75),
which explicitly dropped HowToFit once the parallel fix landed. If
`feature/howtofit-tutorials-1-3` is still open when this is picked up, fold it
there instead of opening a second PR.
